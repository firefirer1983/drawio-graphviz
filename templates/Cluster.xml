        <mxCell id="{{ cell_id }}" value="{{ label }}" style="{{ shape }};whiteSpace=wrap;html=1;" vertex="1" parent="{{ parent }}">
          <mxGeometry x="{{ x_pos }}" y="{{ y_pos }}" width="{{ width }}" height="{{ height }}" as="geometry" />
        </mxCell>

